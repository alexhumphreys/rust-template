services:
  db:
    image: postgres
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: password
  api:
    build: api
    ports:
      - "3000:3000"
    depends_on:
      - db

volumes:
  db-data:
